# 🐧 Установка Linux для запуска Spark Public Server

**Цель:** Установить Elementary OS (Ubuntu-based) на твой компьютер для запуска Spark с Cloudflare Tunnel

## 📋 Что тебе понадобится:

✅ **Блюзут-клавиатура** (у тебя есть)  
✅ **USB-флешка** (минимум 4GB)  
✅ **Проводной интернет** (Ethernet кабель)  
✅ **1-2 часа времени**

## 🖥️ Шаг 1: Подготовка USB флешки

### На Windows (если у тебя есть доступ):

1. Скачай **Rufus** с https://rufus.ie/
2. Скачай Elementary OS с https://elementary.io/ (Elementary 7.1 или 8.0)
3. Запусти Rufus как администратор
4. Выбери Elementary OS ISO файл
5. Выбери USB флешку
6. Нажми **СТАРТ** → Подтверди перезапись

### Через командную строку (если нет GUI):

```cmd
# Скачай ISO (замени на актуальную версию)
curl -o elementary.iso https://sgp1.dl.elementary.io/download/MTQwNDc1OTU4MQ==/elementaryos-7.1-stable.20230814-rc.iso

# Скопируй на USB (X: - буква вашей флешки)
dd if=elementary.iso of=X: bs=4M
```

## 💿 Шаг 2: Установка Elementary OS

### Выбор оборудования:
- **Ноутбук Lenovo/Asus/HP:** Подойдет любая современная модель
- **Память:** Минимум 8GB RAM (лучше 16GB)
- **Место:** Минимум 20GB свободно
- **Процессор:** Intel i3/AMD Ryzen 3 или лучше

### Процесс установки:

1. **Перезагрузка в BIOS:**
   - Включи компьютер
   - Нажми **F2/F10/F12/Del/Esc** (зависит от модели)
   - Выбери USB флешку как первый загрузочный диск

2. **Запуск Elementary OS:**
   - Выбери **Try or Install Elementary**
   - Подождите загрузки (2-3 минуты)

3. **Установка:**
   - Выбери язык: **Русский**
   - Кликни **Установить Elementary**
   - Выбери **Очистить диск и установить Linux** (⚠️ ЭТО УДАЛИТ WINDOWS!)
   - Выбери свой город для времени
   - Создай пользователя: **spark** (пароль простой на первое время)
   - Дождись установки (10-20 минут)

4. **Первая загрузка:**
   - Войди под созданным пользователем
   - Подключи Ethernet кабель к роутеру

## 🔧 Шаг 3: Настройка после установки

### Обновление системы:
```bash
# Обнови систему
sudo apt update && sudo apt upgrade -y

# Установи необходимые инструменты
sudo apt install git curl wget htop neofetch -y
```

### Установка Node.js:
```bash
# Добавь официальный репозиторий Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -

# Установи Node.js 18
sudo apt-get install -y nodejs

# Проверь
node --version  # Должен показать v18.x.x
npm --version   # Должен показать 9.x.x
```

### Настройка Git (для клонирования Spark):
```bash
# Настрой Git
git config --global user.name "Spark Server"
git config --global user.email "server@spark.local"

# Клонируй или скопируй проект Spark
# (Скопируй файлы через USB или скачай из репозитория)
```

## 🌐 Шаг 4: Проверка интернета

```bash
# Проверь подключение
ping -c 4 google.com

# Если нет интернета через Ethernet:
# 1. Проверь кабель
# 2. Попробуй другой порт на роутере
# 3. Сбрось настройки сети: sudo systemctl restart NetworkManager
```

## ⚙️ Шаг 5: Запуск Spark

### Клонируй/скопируй проект:
```bash
# Создай папку проекта
mkdir -p ~/spark
cd ~/spark

# Скопируй все файлы Spark сюда
# или git clone если у тебя есть репозиторий
```

### Запуск:
```bash
# Сделай скрипт исполняемым
chmod +x start-spark-tunnel.sh

# Запусти публичный сервер
./start-spark-tunnel.sh
```

## 🔧 Troubleshooting (Решение проблем)

### Нет звука во время установки:
- Используй наушники или просто продолжай без звука

### Не загружается с USB:
- Попробуй другую флешку
- Проверь в BIOS включен USB boot
- Попробуй Rufus с "DD mode"

### Не распознается клавиатура:
- Подключи проводную USB-клавиатуру
- Или используй on-screen клавиатуру

### Проблемы с сетью в Elementary:
```bash
# Проверь драйверы
sudo apt install bcmwl-kernel-source  # Для Broadcom WiFi (если есть)

# Перезагрузка сетевых настроек
sudo systemctl restart network-manager

# Проверь DNS
echo "nameserver 8.8.8.8" | sudo tee /etc/resolv.conf
```

### Bluetooth клавиатура не работает в установщике:
- Большинство Bluetooth клавиатур работают в Live среде
- Если нет - используй проводную клавиатуру для установки
- После установки Bluetooth заработает

### Система грузится в Windows вместо Linux:
- Зайди в BIOS и измени порядок загрузки
- Выбери Linux как основной

## 💡 Советы для успешной установки

1. **Делай бэкап Windows данных** перед установкой Linux
2. **Проверь BIOS настройки** - иногда USB boot отключен
3. **Используй Ethernet** - WiFi настраивается после установки
4. **Записывай важные моменты** - серийные номера, ключи активации Windows
5. **Не спеши** - установка занимает 10-20 минут

## 🎯 После установки

1. Подключить интернет ✅
2. Установить Node.js ✅
3. Скопировать Spark ✅
4. Выполнить `./start-spark-tunnel.sh` ✅

## 🚨 Если что-то пошло не так

- **Можешь перезагрузиться в Windows** если установка не удалась
- **Попробуй Ubuntu Desktop** вместо Elementary OS
- **Напиши какие ошибки получаешь** - помогу исправить

## 🎉 Финальный результат

Через 1-2 часа у тебя будет:
- Linux компьютер с Elementary OS
- Полноценный Spark публичный сервер
- Контроль над своими данными
- Независимость от облачных платформ

**Готов к установке Spark eco-system? 🔥🚀**

*(Документация создана специально для запуска децентрализованного сервера Spark)*
