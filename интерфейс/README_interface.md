# 🖼️ ПАПКА ИНТЕРФЕЙС - Документация дизайна Spark

## 📋 Цель и структура

Папка `интерфейс` содержит **обязательную документацию** всех интерфейсов приложения Spark в текстовом формате ASCII-схем.

## 🎯 Основные правила

### 🚫 Запрещено:
- Изменять интерфейс без предварительного согласования с автором
- Реализовывать UI изменения без создания/обновления документа в этой папке
- Удалять или игнорировать существующие интерфейсные документы

### ✅ Обязательно:
- Каждый интерфейс документируется в текстовом файле с соответствующим названием
- Использовать ASCII-схемы для визуального представления экранов
- Прописывать colors, размеры, расположение элементов
- Указывать адаптивность для разных экранов
- Многоуровневые элементы с bullet points и отступами

## 📁 Структура файлов

```
интерфейс/
├── README_interface.md           # Этот файл - инструкции
├── feed_screen.md               # Основная лента
├── profile_screen.md            # Профиль пользователя
├── login_screen.md              # Вход в систему
├── search_screen.md             # Поиск
├── create_post_screen.md        # Создание поста
├── comments_sheet.md            # Модальное окно комментариев
├── settings_screen.md           # Настройки
└── ...
```

## 🎨 Формат документации

### 1. Заголовок с описанием
```markdown
# 📱 SCREEN_NAME - Краткое описание назначения
```

### 2. ASCII-схема экрана
```ascii
╭─────────────────────────────────────╮
│ Header content                       │
├─────────────────────────────────────┤
│ Main content                         │
│                                     │
│ ╭─────────────────────────────────╮ │
│ │ Content blocks                   │ │
│ ╰─────────────────────────────────╯ │
╰─────────────────────────────────────╯
```

### 3. Детальные описания элементов
```markdown
## 🎯 Ключевые элементы

### Имя элемента (с иконкой)
- **Свойство**: Значение
- **Еще свойство**: Другое значение
- **Действие**: Что происходит при взаимодействии

### Вторая секция
- Пункты с отступами
- Детализация адаптивности
- Особенности для разных тем
```

### 4. Адаптивность и отзывчивость
```markdown
## 📱 Адаптивные элементы

### Для разных экранов:
- **Аватар**: clamp(min, percentage, max)
- **Шрифты**: Масштабирование
- **Отступы**: Динамически
```

## 🔄 Процесс согласования

### 1. Создание документа
- Создать/обновить файл в папке `интерфейс`
- Детально описать все изменения
- Включить ASCII-схемы

### 2. Отправка на согласование
- Commit в ветку с описанием изменений
- Уведомление: "Интерфейс готов к согласованию"
- Ожидание feedback от автора

### 3. Реализация после согласования
- Получив approval, реализовать в коде
- Сохранить обратную совместимость
- Проверить на всех устройствах

## ⚠️ Важные замечания

- **Все изменения согласовываются заранее**
- **Без согласования = не реализуем**
- **Документ обязателен для каждого UI-изменения**
- **За качество отвечает разработчик интерфейса**

Это система обеспечивает **последовательный и высококачественный дизайн** Spark! ✨

## 📞 Контакты по согласованию

Для всех вопросов по интерфейсам - обращаться напрямую к автору перед разработкой.
