# Spark Server

Сервер для приложения Spark, построенный исключительно на кастомном API без использования внешних сервисов.

## 🚀 Быстрый запуск

### Два клика для запуска:
1. Убедитесь, что Node.js установлен
2. Запустите команду:
```bash
npm install
npm start
```

Сервер будет доступен на `http://localhost:3000`

## 📋 Архитектура

### Структура проекта
```
server-app/
├── index.js              # Основной серверный файл
├── package.json          # Зависимости
├── routes/               # API маршруты
│   ├── auth.js          # Аутентификация
│   ├── users.js         # Пользователи
│   ├── posts.js         # Посты
│   ├── messages.js      # Сообщения
│   ├── notifications.js # Уведомления
│   ├── upload.js        # Загрузка файлов
│   ├── comments.js      # Комментарии
│   └── analytics.js     # Аналитика
├── models/               # Модели данных (планируется)
├── uploads/              # Загруженные файлы
└── README.md            # Этот файл
```

### База данных
Используется SQLite для локального хранения данных без зависимости от облачных сервисов.

### Таблицы
- users: Пользователи
- posts: Посты
- messages: Сообщения
- notifications: Уведомления
- comments: Комментарии

## 🔧 API Endpoints

### Аутентификация
- `POST /auth/register` - Регистрация
- `POST /auth/login` - Вход
- `GET /auth/profile` - Профиль пользователя
- `PUT /auth/profile` - Обновление профиля
- `POST /auth/device-token` - Регистрация device token

### Пользователи
- `GET /users/search?q=query` - Поиск пользователей
- `GET /users/:id` - Информация о пользователе

### Посты
- `GET /posts/feed` - Лента постов
- `GET /posts/:id` - Отдельный пост
- `POST /posts/create` - Создание поста

### Сообщения
- `GET /messages/conversation/:userId` - Конверсация с пользователем
- `POST /messages/send` - Отправка сообщения

### Уведомления
- `GET /notifications` - Получение уведомлений

### Комментарии
- `GET /posts/:postId/comments` - Комментарии к посту
- `POST /comments` - Добавление комментария
- `DELETE /comments/:commentId` - Удаление комментария

### Файлы
- `POST /upload` - Загрузка одного файла
- `POST /upload/multiple` - Загрузка нескольких файлов

### Аналитика
- `GET /analytics/user/:userId` - Аналитика пользователя
- `GET /smm/stats` - SMM статистика платформы

## 🔒 Безопасность

- JWT токены для аутентификации
- Bcrypt для хеширования паролей
- Валидация всех входных данных
- Защита от SQL инъекций через параметризованные запросы

## 📱 Реальное время

Socket.IO используется для:
- Мгновенные сообщения (шифрованные E2E)
- Уведомления в реальном времени
- Онлайн статусы

## 🚫 Правила разработки

- Никак не использовать Firebase, Firestore или внешние облачные сервисы
- Все данные через внутренний API
- Безопасность выше всего
- Соответствие правилам Spark

## 📊 Масштабируемость

Сервер оптимизирован для минимальных нагрузок с высокой функциональностью, обеспечивая экономию ресурсов для недорогого хостинга.

## 🌍 Локализация

Все тексты и ответы на русском языке для соответствия требованиям Spark.

## 🔧 Мониторинг

- Логи сервера в консоли
- Статистика ошибок
- Мониторинг производительности

---

Сервер готов к развертыванию на любом персональном ПК без дополнительных настроек!
